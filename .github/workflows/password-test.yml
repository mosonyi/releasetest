name: Password test

on:
  workflow_dispatch:
    inputs:
      name:
        description: "Password (foobar)"
        default: ""

jobs:
    hello:
        runs-on: ubuntu-latest

        steps:
        - name: Echo Step
          run: echo "Password input is ${{ github.event.inputs.name }}"

        - name: Test pass
          env:
            CERTIFICATE_BASE64: ${{ secrets.PRIVKEY_B64 }}
            PASS: ${{ github.event.inputs.name }}
          run: |
            echo $CERTIFICATE_BASE64 > privkey.pem.base64
            base64 -i --decode privkey.pem.base64 > privkey.pem
            openssl rsa -in privkey.pem -passin env:PASS -pubout -out privkey.pub